import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const DueReducer = createSlice({
  name: 'dueReducer',
  initialState,
  reducers: {}
});

export const {} = DueReducer.actions

export default DueReducer.reducer