import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const BudgetReducer = createSlice({
  name: 'budgetReducer',
  initialState,
  reducers: {}
});

export const {} = BudgetReducer.actions

export default BudgetReducer.reducer