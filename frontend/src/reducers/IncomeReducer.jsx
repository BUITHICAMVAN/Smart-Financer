import { createSlice } from '@reduxjs/toolkit'

const initialState = {
}

const IncomeReducer = createSlice({
  name: 'incomeReducer',
  initialState,
  reducers: {}
});

export const {} = IncomeReducer.actions

export default IncomeReducer.reducer