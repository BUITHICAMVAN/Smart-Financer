# Save a snapshot of the dataset every 24 hours (86400 seconds) if at least 1 key has changed
save 86400 1

# Set the maximum memory usage and policy (optional)
maxmemory 256mb
maxmemory-policy allkeys-lru